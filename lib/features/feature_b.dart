import 'package:flutter/material.dart';

class FeatureBWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: Center(
        child: Text('Feature A'),
      ),
    );
  }
}
